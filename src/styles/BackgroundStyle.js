import {StyleSheet} from 'react-native';

const background = StyleSheet.create({
  backgroundImage: {
    flex: 1,
    resizeMode: 'cover',
    alignItems: 'center',
  },
});

export default background;
