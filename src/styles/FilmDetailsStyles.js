import {StyleSheet} from 'react-native';
import {metrics} from '../constants/index';

const FilmDetailsStyles = StyleSheet.create({
  mainContainer: {
    flex: metrics.size1,
    justifyContent: 'space-evenly',
    alignItems: 'center',
  },
});

export default FilmDetailsStyles;
